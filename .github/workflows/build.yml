name: Build
on:
  push:
    branches:
      - master
  schedule:
    - cron: '0 0 * * 4'

jobs:
  archlinux-base:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker build archlinux-base -t openturns/archlinux-base
      - name: Upload
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: ./deploy.sh openturns/archlinux-base

  archlinux-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker build -t openturns/archlinux-python:latest archlinux-python
      - name: Upload
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: ./deploy.sh openturns/archlinux-python:latest

  archlinux-mingw:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker build -t openturns/archlinux-mingw:latest archlinux-mingw
      - name: Upload
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: ./deploy.sh openturns/archlinux-mingw:latest

  archlinux-module:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker build -t openturns/archlinux-module:latest archlinux-module
      - name: Upload
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: ./deploy.sh openturns/archlinux-module:latest

  archlinux-module-mingw:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker build -t openturns/archlinux-module-mingw:latest archlinux-module-mingw
      - name: Upload
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: ./deploy.sh openturns/archlinux-module-mingw:latest

  manylinux1_x86_64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker build -t openturns/manylinux1_x86_64:latest manylinux1_x86_64
      - name: Upload
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: ./deploy.sh openturns/manylinux1_x86_64:latest
